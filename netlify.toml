# Netlify Configuration File
# Configure build settings, deploy contexts, redirects, headers, and functions
# read more at - https://www.netlify.com/docs/netlify-toml-reference/

# The following redirect is intended for use with most SPAs that handle
# routing internally.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200